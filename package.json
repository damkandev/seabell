{
  "name": "seabell",
  "version": "0.1.0",
  "private": true,
  "description": "Espacio de trabajo local-first para revisar y anotar documentos PDF.",
  "license": "PolyForm-Noncommercial-1.0.0",
  "repository": {
    "type": "git",
    "url": "https://github.com/damkandev/seabell.git"
  },
  "bugs": {
    "url": "https://github.com/damkandev/seabell/issues"
  },
  "homepage": "https://github.com/damkandev/seabell#readme",
  "scripts": {
    "dev": "pnpm --parallel --filter @seabell/web --filter @seabell/api dev",
    "build": "pnpm --parallel --filter @seabell/web --filter @seabell/api build",
    "start": "pnpm --parallel --filter @seabell/web --filter @seabell/api start",
    "db:generate": "pnpm --filter @seabell/api db:generate",
    "db:migrate": "pnpm --filter @seabell/api db:migrate",
    "lint": "pnpm --filter @seabell/web lint",
    "typecheck": "pnpm --parallel --filter @seabell/web --filter @seabell/api typecheck",
    "test": "pnpm --filter @seabell/web test",
    "test:api": "pnpm --filter @seabell/api test",
    "test:unit": "pnpm --filter @seabell/web test:unit",
    "test:e2e": "pnpm --filter @seabell/web test:e2e"
  },
  "packageManager": "pnpm@10.33.0"
}

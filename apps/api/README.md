# Seabell API

Este directorio está reservado para el backend independiente de Seabell.

Las rutas API pequeñas pueden seguir viviendo en `apps/web/src/app/api/`. Añade aquí el primer servicio que necesite ciclo de despliegue, secretos o runtime independiente; no introduzcas un framework hasta definir ese contrato.
